# =============================================================================
# 澳大利亚房贷AI助手 - 环境配置示例
# =============================================================================
# 说明：
# 1. 复制此文件为 .env 并填入真实的API密钥
# 2. 对于Streamlit Cloud部署，在 Secrets 管理中配置这些变量
# 3. 必需配置：OPENAI_API_KEY
# 4. 可选配置：其他所有变量都有默认值

# =============================================================================
# 必需配置
# =============================================================================

# OpenAI API密钥（必需）
# 获取地址：https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# 可选配置 - 模型设置
# =============================================================================

# 使用的模型名称（推荐：gpt-5-mini，内置Web Search）
# 可选：gpt-5-mini（推荐），兼容 gpt-4o-mini（无内置搜索）
MODEL_NAME=gpt-5-mini

# 提供商选择（默认：openai）
MODEL_PROVIDER=openai

# =============================================================================
# 说明：网络搜索
# =============================================================================
# 使用 gpt-5-mini 时，无需额外搜索API密钥，模型内置 Web Search 工具。

# =============================================================================
# 可选配置 - RAG功能（已归档）
# =============================================================================

# 是否启用RAG检索功能（默认：false）
RAG_ENABLED=false

# RAG检索数量（默认：3）
RAG_TOP_K=3

# 向量数据库存储目录（默认：data/chroma）
CHROMA_DIR=data/chroma

# OpenAI嵌入模型（默认：text-embedding-3-small）
EMBEDDING_MODEL=text-embedding-3-small
