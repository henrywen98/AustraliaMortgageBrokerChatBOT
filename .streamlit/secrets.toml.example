# =============================================================================
# Streamlit Cloud Secrets 配置示例
# =============================================================================
# 使用说明：
# 1. 登录 https://share.streamlit.io/
# 2. 找到你的应用 -> Settings -> Secrets
# 3. 将下面的内容复制到 Secrets 框中
# 4. 替换 your_actual_api_key 为真实的API密钥
# 5. 保存并重新部署应用

# =============================================================================
# 必需配置
# =============================================================================

# OpenAI（默认）
OPENAI_API_KEY = "your_actual_openai_api_key_here"

# 或 Azure OpenAI（开启下列配置，并删除上方 OPENAI_API_KEY）
# MODEL_PROVIDER = "azure"
# AZURE_OPENAI_ENDPOINT = "https://<resource>.cognitiveservices.azure.com/openai/v1/"
# AZURE_OPENAI_API_KEY = "your_azure_key"
# AZURE_OPENAI_DEPLOYMENT = "gpt-5-mini"

# =============================================================================
# 可选配置
# =============================================================================

# 默认模型名称（推荐 gpt-5-mini，内置 Web Search）
MODEL_NAME = "gpt-5-mini"

# 其他历史配置（通常无需修改）
# RAG 功能已停用，如需启用请同步更新代码
